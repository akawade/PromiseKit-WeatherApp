 platform :ios, '9.0'

target 'WeatherApp' do
  use_frameworks!

  # Pods for WeatherApp
     pod "PromiseKit", "~> 6.0"
     pod "PromiseKit/CoreLocation", "~> 6.0"

target 'WeatherAppTests' do
    inherit! :search_paths
 end

end
